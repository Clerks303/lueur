/**
 * English stub — intentionally empty. French is the primary locale.
 * When we open to UK/US we translate from fr.ts; until then i18n-js
 * falls back to French for any key a non-fr device requests.
 */
const en = {} as const;

export default en;
