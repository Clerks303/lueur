/** Screen 5 — Cross-domain reveal (stub, week 2). */
import { Text, View } from "react-native";

export default function OnboardingCrossDomain() {
  return (
    <View className="flex-1 bg-bg-app items-center justify-center px-8">
      <Text className="font-serif italic text-text-secondary">
        Screen 5 — cross-domain (stub)
      </Text>
    </View>
  );
}
