/** Screen 2 — Photo capture (stub, implemented at T10/T11). */
import { Text, View } from "react-native";

export default function OnboardingPhoto() {
  return (
    <View className="flex-1 bg-bg-app items-center justify-center px-8">
      <Text className="font-serif italic text-text-secondary">
        Screen 2 — photo capture (stub)
      </Text>
    </View>
  );
}
