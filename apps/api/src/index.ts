// Hono + Bun entry point. Populated at T04.
// Reminder: listen on process.env.API_HOST (0.0.0.0 in dev) and process.env.API_PORT
// so a physical iPhone on the LAN can reach it. See .env.example.
export {};
