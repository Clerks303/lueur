{
  "name": "@lueur/worker",
  "version": "0.0.0",
  "private": true,
  "description": "BullMQ worker (Bun runtime). Consumes the analyze-photo queue and others.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no tests yet)\" && exit 0"
  },
  "dependencies": {
    "@lueur/shared-types": "workspace:*",
    "@lueur/db": "workspace:*",
    "@lueur/prompts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:"
  }
}
